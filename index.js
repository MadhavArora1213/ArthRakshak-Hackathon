import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent calls AppRegistry.registerComponent('main', () => App);
// It also ensures that whether you load the app in Expo Go or in a native build,
// the environment is set up appropriately
registerRootComponent(App);



// Now write a Prompt based on the color pallete thing 
// the first focus of promp tto create the color pallete  file and in the prompt mention the path also for accurate output 

// and write a proper prompt i am giving to the copilot 
